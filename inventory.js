let items=[];
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
function render(){const q=document.getElementById('search').value.toLowerCase();const pub=document.getElementById('publisher').value;const st=document.getElementById('status').value;const filtered=items.filter(x=>{const hay=[x.sku,x.title,x.issue,x.significance,x.publisher,x.grade].join(' ').toLowerCase();return(!q||hay.includes(q))&&(!pub||x.publisher===pub)&&(!st||x.status.startsWith(st));});document.getElementById('count').textContent=filtered.length;document.getElementById('grid').innerHTML=filtered.map(x=>{const photo=x.photos?.front;const img=photo?`<div class="card-image"><img src="${photo}" alt="${x.title} ${x.issue}"><span class="actual-tag">ACTUAL ITEM</span></div>`:`<div class="placeholder"><span>${x.sku}</span><small>PHOTO PENDING</small></div>`;const link=x.slug||'#';const value=(x.low||x.high)?`${money(x.low)}–${money(x.high)}`:'Value TBD';return `<article class="item-card ${photo?'photographed':''}"><a href="${link}">${img}<div class="item-card-body"><div class="sku-line"><span>${x.sku}</span>${x.grade?`<b>${x.grade}</b>`:''}</div><h3>${x.title}</h3><div class="issue">${x.issue}</div><p>${x.significance}</p><div class="item-bottom"><span>${x.year||'Year TBD'} · ${x.publisher||'Publisher TBD'}</span><strong>${value}</strong></div></div></a></article>`}).join('');}
fetch('data/inventory.json').then(r=>r.json()).then(d=>{items=d;const pubs=[...new Set(items.map(x=>x.publisher).filter(Boolean))].sort();document.getElementById('publisher').innerHTML+=pubs.map(p=>`<option>${p}</option>`).join('');['search','publisher','status'].forEach(id=>document.getElementById(id).addEventListener(id==='search'?'input':'change',render));render();});