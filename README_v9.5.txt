KAC Vault v9.5 — Featured-Fit Desktop Vault

- Uses the full-composition sizing seen in the preferred Featured view
- Desktop vault is intentionally inset to 92% of viewport width
- Preserves full left copy, central vault, right copy, top navigation, and bottom navigation
- Adds intentional dark space above/below instead of cropping
- Closed and open vault states use identical stage dimensions
- Navigation hotspots remain locked to the artwork
- Mobile behavior remains full-width
- All collection, memories, inventory, JSON, analytics, and permanent SKU work preserved
